/*
    - Alias de tipos
*/

// type nombre = tipo;

type coordenada = [number, number]; // tupla
const coordenadaTelematica: coordenada = [10, 20];

type calidad = "baja" | "media" | "alta"; // union
const calidadVideo: calidad = "alta";